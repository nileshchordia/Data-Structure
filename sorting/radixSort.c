// it sort the digit decimal by decimal like ones,tens,hundred and so on--- of each digit---
// and apply stable sorting algorithm because range is base(10 for decimal)
// for decimal, base=10
// for octal, base=8

#include <stdio.h>

int main(int argc, char const *argv[])
{
	/* code */
	return 0;
}